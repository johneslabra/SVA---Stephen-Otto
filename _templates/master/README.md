# Blank copies of the client detail files

These are the empty versions the client folder starts with. They are here so the
AI can tell, exactly rather than by guesswork, which client details have been
filled in and which are still untouched.

Do not fill these in. Fill in the real ones in `master/`.
